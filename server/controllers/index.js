const controllers = {
  reset: require("./reset"),
  auth: require("./auth"),
  pointOfInterest: require("./pointOfInterest"),
  middleware: require("./middleware"),
};

module.exports = controllers;
